# IP address of the Gravotech marking machine
GRAVEUSE_IP=127.0.0.1

# Communication port for the machine (default is usually 3000)
GRAVEUSE_PORT=3000

# Application environment (e.g., dev, staging, prod)
ENVIRONMENT=dev